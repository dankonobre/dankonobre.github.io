---
import Container from './Container.astro';

const { class: className, ...props } = Astro.props;
---

<section class={`p-4 flex w-full justify-center ${className}`} {...props}>
  <Container>
    <slot />
  </Container>
</section>
